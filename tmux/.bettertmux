#!/bin/bash

# 

source ~/.dotfiles/colors.sh

get() {
    local option=$1
    local default_value=$2
    local option_value="$(tmux show-option -gqv "$option")"

    if [ -z "$option_value" ]; then
       echo "$default_value"
    else
       echo "$option_value"
    fi
}

set() {
    local option=$1
    local value=$2
    tmux set-option -gq "$option" "$value"
}

setw() {
    local option=$1
    local value=$2
    tmux set-window-option -gq "$option" "$value"
}

set "status" "on"
set "status-justify" "left"

set "status-left-length" "100"
set "status-right-length" "100"
set "status-right-attr" "none"

set "message-fg" "$normalwhite"
set "message-bg" "$BackgroundColor"

set "message-command-fg" "$normalwhite"
set "message-command-bg" "$BackgroundColor"

set "status-attr" "none"
set "status-left-attr" "none"

setw "window-status-fg" "$BackgroundColor"
setw "window-status-bg" "$BackgroundColor"
setw "window-status-attr" "none"

setw "window-status-activity-bg" "$BackgroundColor"
setw "window-status-activity-fg" "$BackgroundColor"
setw "window-status-activity-attr" "none"

setw "window-status-separator" ""


set "pane-border-fg" "$normalwhite"
set "pane-active-border-fg" "$normalwhite"

set "display-panes-active-colour" "$brightyellow"
set "display-panes-colour" "$normalblue"

set "status-bg" "$BackgroundColor"
set "status-fg" "$normalwhite"

set "@prefix_highlight_fg" "$BackgroundColor"
set "@prefix_highlight_bg" "$normalgreen"
set "@prefix_highlight_copy_mode_attr" "fg=$BackgroundColor,bg=$normalgreen"
set "@prefix_highlight_output_prefix" "  "

spotify_bar="\
#[fg=$normalwhite,bg=$BackgroundColor]\
 #{spotify_artist}: #{spotify_track} "

battery_bar="\
#[fg=$normalwhite,bg=$BackgroundColor, bold]\
 #{battery_percentage} \
#[fg=#{battery_status_bg},bg=$BackgroundColor]\
 \
"

time_bar="\
#[fg=$normalwhite,bg=$BackgroundColor, bold]\
 \
#[fg=$normalwhite,bg=$BackgroundColor, nobold]\
%a %d.%b %H:%M  \
#[fg=$normalwhite,bg=$BackgroundColor, bold]\
  \
   "

set "status-right" "${battery_bar}${time_bar}"
#set "status-right" "${spotify_bar}${battery_bar}${time_bar}"

set "status-left" "\
#[fg=$normalblue,bg=$normalwhite,bold]\
  \
 \
#[fg=$BackgroundColor,bg=$normalwhite,bold]\
#S\
  \
#[fg=$normalwhite,bg=$BackgroundColor,nobold,nounderscore,noitalics]\
\
 \
#[fg=$brightmagenta,bg=$BackgroundColor,bold,nounderscore,noitalics]\
 \
#[fg=$normalwhite,bg=$BackgroundColor,bold,nounderscore,noitalics]\
$(whoami)\
  \
"

set "window-status-format" "\
#[fg=$normalwhite,bg=$BackgroundColor, nobold]\
 \
\
 \
#W \
#[fg=$normalwhite,bg=$BackgroundColor, bold]\
 "

set "window-status-current-format" "\
#[fg=$normalwhite,bg=$BackgroundColor,nobold,nounderscore,noitalics]\
 \
#[fg=$normalyellow,bg=$BackgroundColor,nobold]\
 \
 \
#[fg=$normalwhite,bg=$BackgroundColor, bold]\
 "
